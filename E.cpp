#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string h, e;
    cin >> h >> e;

    if (h == e) {
        cout << h << "\n"; 
    } else {
        cout << "1\n"; 
    }

    return 0;
}